namespace DBison.Core.Entities;
public class DBisonView : DatabaseObjectBase
{
    public DBisonView(string name, ServerInfo server, DatabaseInfo dataBase) : base(name, server, dataBase)
    {
    }
}
